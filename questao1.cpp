#include <stdio.h>
#include <conio.h>
	
int main()
{	
	char texto;
	int i;
	
	for(i = 0; texto[i] != '\0' && i < sizeof(texto); i++) {

	}

	printf("%d", i);
	printf("O texto possui %d caracteres.", i);

	return 0;
}
